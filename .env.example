# Acheron Docker Compose configuration
# Copy to .env and customize as needed.

# Shared secret for worker registration.
# Workers must send this token in Authorization: Bearer <token> header.
# Leave empty or unset to allow open registration (dev mode only).
ACHERON_REGISTRATION_TOKEN=dev-registration-token
